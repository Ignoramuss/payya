[package]
name = "payya-quantization"
description = "Post-training quantization (Int8) for reduced memory and faster inference"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
